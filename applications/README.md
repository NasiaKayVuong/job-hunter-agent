# applications/

Drafted resumes/cover letters (one folder per job) and `log.md`, an audit trail of
every search run and application attempt, land here. This whole directory is
gitignored — it's personal output, not part of the tool.
